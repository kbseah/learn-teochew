# Scripts for parsing Teochew pronunciations in Wiktionary

Download English Wiktionary database dump: https://dumps.wikimedia.org/enwiktionary/latest/enwiktionary-latest-pages-articles.xml.bz2

Use the script `wiktionary_teochew_parse.py` to extract headwords with Teochew pronunciations (in Peng'im romanization) and export either into JSON format, and as preformatted files in Markdown format for the study guide.

Use the script `convert_pengim.py` to convert Peng'im to Flattened IPA or IPA phonetic spelling.

The file `pengim_utils.py` contains functions 

To view help message, use the `--help` flag with the above scripts.
